import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "KuDE Express - Visualizador de Facturas e-Kuatia",
  description: "Visualiza, imprime y descarga tus facturas electrónicas de la SET (e-Kuatia) en formato limpio y profesional.",
  icons: {
    icon: '/logo.png', // Asumiendo que logo.png sirve como favicon o hay un favicon.ico
  }
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
